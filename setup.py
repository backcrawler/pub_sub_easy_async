from setuptools import find_packages, setup

setup(
    name='pub_sub_easy_async',
    packages=find_packages(),
    version='0.1.0',
    description='Async Pub-Sub in pure Python',
    author='backcrawler',
    install_requires=[],
    license='MIT',
)
